#!/usr/bin/env sh
# LLM API keys are NOT set here (no secrets in p3, no pinentry at login).
# Source of truth: pass entries env/* — exported by mise via
# ~/.config/mise/conf.d/00-secrets.toml on shell activation.
